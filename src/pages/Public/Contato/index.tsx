import PublicPage from "../PublicPage";

/**
 * Página pública — Contato
 *
 * Canais de contato por finalidade: comercial, suporte técnico,
 * privacidade de dados e parcerias/imprensa.
 * Cada canal com e-mail e orientações para agilizar o atendimento.
 *
 * Acesso público em: /contato
 */
const ContatoPage = () => <PublicPage pageKey="contato" />;

export default ContatoPage;
