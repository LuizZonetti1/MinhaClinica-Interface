import PublicPage from "../PublicPage";

/**
 * Página pública — Blog
 *
 * Conteúdo editorial sobre gestão clínica, indicadores,
 * experiência do paciente e tecnologia na saúde.
 *
 * Acesso público em: /blog
 */
const BlogPage = () => <PublicPage pageKey="blog" />;

export default BlogPage;
