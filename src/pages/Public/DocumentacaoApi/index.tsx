import PublicPage from "../PublicPage";

/**
 * Página pública — Documentação API
 *
 * Visão geral da API REST da plataforma:
 * autenticação JWT, módulos de usuários, agendamentos e documentos.
 * Orientação para solicitar acesso técnico antecipado.
 *
 * Acesso público em: /documentacao-api
 */
const DocumentacaoApiPage = () => <PublicPage pageKey="documentacao-api" />;

export default DocumentacaoApiPage;
