import PublicPage from "../PublicPage";

/**
 * Página pública — Preços
 *
 * Apresenta os planos disponíveis (Clínica e Consultório),
 * modelo de cobrança por perfil ativo e orientação para solicitar proposta.
 *
 * Acesso público em: /precos
 */
const PrecosPage = () => <PublicPage pageKey="precos" />;

export default PrecosPage;
